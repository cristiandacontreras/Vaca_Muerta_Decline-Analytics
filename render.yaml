services:
  - type: web
    name: vaca-muerta-analytics
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python dashboard/app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0